{
  "_comment": "Scan allowlist for skill-manager self-scan. Each entry matches file patterns to rules. Only matches documentation examples and test fixtures — never real secrets.",
  "allowlist": [
    {
      "file": "tests/SkillManager.Cli.Tests/SecurityScannerTests.cs",
      "rules": ["EMAIL_EXPOSED", "ABSOLUTE_USER_PATH", "EXTERNAL_CDN", "GITHUB_TOKEN", "PRIVATE_KEY", "API_KEY", "JWT_TOKEN"],
      "reason": "Test fixtures — intentionally contain test data to verify scanner detection"
    },
    {
      "file": "tests/SkillManager.Cli.Tests/*",
      "rules": ["*"],
      "reason": "All test files — test data is expected, not a real risk"
    },
    {
      "file": "tools/SkillManager.Cli/Tools/SecurityScanner.cs",
      "rules": ["HTTP_URL", "HOME_PATH_REFERENCE", "EXTERNAL_CDN"],
      "reason": "Scanner source code contains regex patterns used for detection"
    },
    {
      "file": "tools/SkillManager.Cli/assets/viewer.html",
      "rules": ["UNSAFE_INNERHTML"],
      "reason": "Static HTML strings only. No user/model data in innerHTML."
    }
  ]
}
