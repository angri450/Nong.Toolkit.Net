# Prerequisites

## Quick Check

```bash
dotnet --version
```

If missing, install .NET SDK from https://dotnet.microsoft.com/download.

## Dependencies

| Dependency | Required For | Severity |
|-----------|-------------|----------|
| .NET SDK | All CLI operations | **Blocking** |

No other dependencies. All operations via `skill-manager` global tool.
