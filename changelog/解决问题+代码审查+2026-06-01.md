# 解决问题 — 代码审查 — 2026-06-01

## 修复列表

### 1. FIXED — multimodal/SKILL.md:3-6 — description 更新

在 frontmatter description 中添加 `image structure analysis` 和 `pure .NET ImageAnalyzer`，
确保 Claude Code 能正确触发 skill 处理图像分析请求。

### 2. FIXED — multimodal/SKILL.md:28 — "layout" 关键词冲突

规则 3 中移除 `"layout"` 关键词，改为 `"document formatting"`。
规则 3（Cloud OCR）保留 `"Word", "docx"` 作为主要触发词，`"document formatting"` 覆盖版式相关请求。
规则 4（ImageAnalyzer）保留 `"layout"` 用于图像空间分布分析。

### 3. FIXED — multimodal/SKILL.md:30 — "Both Cloud + Local" 语义澄清

改为 `"Both OCR modes (Cloud + Local)"`，明确排除 ImageAnalyzer，避免三能力下的歧义。

### 4. FIXED — multimodal/references/image-analyzer.md:45 — AsciiMap 维度描述

从 `"每个字符代表一列像素"` 改为 `"多行网格，每行对应图像的一行像素带，每个字符代表 targetWidth 像素宽的列"`。
同时更新快速开始中的注释以保持一致。

### 5. FIXED — multimodal/references/image-analyzer.md:69-78 — 像素分类优先级

添加 `"按表格顺序判定，命中即停止"` 说明。
补充未命中四类的像素（亮度 101–240 且饱和度 ≤ 30）归为空白的处理规则。

### 6. FIXED — multimodal/SKILL.md:99 — Workspace 节补充 ImageAnalyzer 引用

从 `"Write a Program.cs with the OCR task. See ocr-cloud.md"` 改为
`"Write a Program.cs with the task. For OCR: see ocr-cloud.md. For image analysis: see image-analyzer.md."`

### 7. FIXED — diagram/references/workspace-setup.md:25-27 — PowerShell 路径引号

`cd ~/Documents/GroundPA Toolkit Workplace/diagram` → `Set-Location "$HOME\Documents\GroundPA Toolkit Workplace\diagram"`
`cd DiagramWriter` → `Set-Location DiagramWriter`
使用 `Set-Location` + 引号包裹含空格路径，确保 PowerShell 正确解析。

### 8. FIXED — diagram/references/workspace-setup.md:142-152 — FAQ 补充可操作建议

在原有说明基础上增加一段：节点很多时可增大 width/height 以获得更多布局计算空间，
但最终 PNG 仍会裁剪到内容 + 内边距。给用户一个实际可用的调节方向。
