# === DO NOT UPLOAD ===
# All development files (CLAUDE.md, AGENTS.md, PROJECT_STATE.md, logs, changelogs,
# docs, install scripts, references, skill-creator source) are consolidated under
# .claude/ which is gitignored below. Do not add them back to the repo root.
#
# Root-level CLAUDE.md and AGENTS.md entries below are safety nets only —
# in case tools regenerate them at root. The canonical copies live in .claude/.

# Project management files — canonical copies in .claude/
CLAUDE.md
Claude.md
claude.md
CLAUDE.local.md
claude.local.md
AGENTS.md

# Claude Code local config (covers all dev files consolidated under .claude/)
.claude/
.codex/

# Archive and backup material
_archive/
Nong.Toolkit_archive/
_v1_backup/

# Build, package, and test outputs
bin/
obj/
build/
dist/
TestResults/
coverage/
.coverage
*.skill
*.zip
*.nupkg
*.tmp
*.bak
*.orig
.skill/
.security-scan-passed

# Python and test caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.pyc
*.pyo

# Local dependency caches and environments
.venv/
venv/
node_modules/

# IDE, editor, and OS noise
.vs/
.vscode/
.idea/
*.user
*.suo
.DS_Store
Thumbs.db
