# 发现问题 — 代码审查 — 2026-06-01

## 审查范围

GroundPA Toolkit 工作树 diff：17 个文件修改 + 1 个新文件（multimodal/references/image-analyzer.md）。
变更主题：.NET 版本策略 10/11 → 8.0+、中文渲染文档、ImageAnalyzer 新能力、画布自适应文档。

## 发现列表（按严重度排序）

### 1. HIGH — multimodal/SKILL.md:6 — 前端 description 未更新

description 仍为 `"cloud API + local PaddleOCR CPU"`，未提及 ImageAnalyzer。
description 是 Claude Code 判断是否激活 skill 的主要信号，纯图像分析请求可能不会触发该 skill。

### 2. HIGH — multimodal/SKILL.md:29-30 — "layout" 关键词冲突

规则 3: `"Word", "docx", "layout", "formatting"` → Cloud OCR
规则 4: `"analyze", "structure", "layout", "white space"` → ImageAnalyzer

顺序匹配下规则 3 优先。"分析图像布局"会被错误路由到 Cloud OCR。规则 3 的 "layout" 指 Word 文档版式，规则 4 的 "layout" 指图像空间分布，但关键词本身未区分。

### 3. MEDIUM — multimodal/SKILL.md:31 — "Both Cloud + Local" 语义模糊

现有 3 个能力（Cloud/Local/ImageAnalyzer），但文本写 "Both" 暗示只有 2 个。

### 4. MEDIUM — multimodal/references/image-analyzer.md:45 — AsciiMap 维度描述不清

"每个字符代表一列像素" 可被理解为单行横条（1D）或二维网格（2D）。应明确说明是多行 ASCII 艺术网格。

### 5. MEDIUM — multimodal/references/image-analyzer.md:75-76 — 像素分类条件重叠

饱和度 > 30（图形）与亮度 40-100（边框）可同时满足。分类优先级未声明。

### 6. MEDIUM — multimodal/SKILL.md:101 — Workspace 节只引用 ocr-cloud.md

`See [ocr-cloud.md](references/ocr-cloud.md) for the full template.` 未提及 image-analyzer.md。

### 7. LOW — diagram/references/workspace-setup.md:25 — PowerShell 含空格路径未加引号

`cd "<workplace>/diagram"` 在 PowerShell 中如果缺少引号会被解析为多个参数。同块中的 `mkdir -p` 已修复但 `cd` 遗漏。

### 8. LOW — diagram/references/workspace-setup.md:142-150 — FAQ 失去可操作建议

FAQ 解释了 width/height 不决定输出尺寸，但未说明用户如何控制输出尺寸。
