# 解决问题：2.0.0 Nong CLI Skill Layer

日期：2026-06-03

## 修复内容

### 1. 切换到 Nong CLI-first

- 重写 `word/SKILL.md`
- 重写 `excel/SKILL.md`
- 重写 `chart/SKILL.md`
- 重写 `diagram/SKILL.md`
- 重写 `genre/SKILL.md`

这些 skill 现在只调用当前 `nong` CLI 已实现命令，不再默认创建临时 .NET workspace。

### 2. 新增已实现命令对应 skill

- 新增 `inspect/SKILL.md`
- 新增 `icons/SKILL.md`

`inspect` 覆盖：

```text
inspect diagnose
inspect refs
inspect write-paper
```

`icons` 覆盖：

```text
icons list
icons search
```

### 3. 停用未实现能力

- 删除 `pptx/SKILL.md`
- 删除 `multimodal/SKILL.md`
- 新增 `pptx/README.md`
- 新增 `multimodal/README.md`

PPTX/OCR 保留资料目录，但不再作为 2.0.0 skill 暴露。

### 4. 更新插件与安装文档

- `.claude-plugin/plugin.json` 升级到 `2.0.0`
- `.claude-plugin/marketplace.json` 更新描述
- `skills.sh.json` 升级到 `2.0.0`
- `README.md` / `README.zh-CN.md` 改为 `nong` CLI 安装和调用方式
- `DEVELOP.md` 更新 2.0.0 边界

### 5. 安全扫描修复

- `email/SKILL.md` 示例邮箱改为占位符
- `email/references/github-triage.md` 不再硬编码域名地址
- `skill-manager/references/security-guide.md` 避免示例触发私钥/CDN/路径规则
- `skill-manager/references/sanitization_checklist.md` 改写路径扫描示例
- `skill-manager/workflows/session/workflow.md` 和 `wrapper/workflow.md` 改写绝对路径示例
- `skill-manager/assets/eval_review.html` 用 DOM API 替代动态 `innerHTML`

## 验证

### PASS — JSON 清单解析

`.claude-plugin/plugin.json`、`.claude-plugin/marketplace.json`、`skills.sh.json` 均可被 PowerShell `ConvertFrom-Json` 解析。

### PASS — 插件清单与实际 skill 目录一致

`.claude-plugin/plugin.json` 声明 17 个 skill，和实际含 `SKILL.md` 的目录一致。

### PASS — Claude 插件校验

```text
claude plugin validate .
Validation passed
```

### PASS — 17 个 skill 逐个 validate

使用本地 `Angri450.Nong/SkillManager` 的 net8.0 项目逐个验证，17 个 skill 全部 `Valid: YES`。

### PASS — 安全扫描无 High/Critical

使用本地 `Angri450.Nong/SkillManager` 的 net8.0 项目扫描仓库：

```text
Findings: 15
Medium: 14
Low: 1
Exit code: 0
```

剩余项为历史文档 home path 提示和 Apache license HTTP URL，未阻塞打包。

## 注意

全局安装的 `skill-manager.exe` 当前依赖 `.NET 11 preview runtime`，本机只有 .NET 8 和 .NET 10，因此不能直接运行。已改用源码中的 net8.0 `SkillManager.Cli.csproj` 完成验证。
